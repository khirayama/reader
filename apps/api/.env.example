# Database - Docker Compose PostgreSQL設定（即座に動作）
DATABASE_URL="postgresql://postgres:password@localhost:5432/rss_reader?schema=public"

# JWT - 開発専用シークレット（本番環境では必ず変更）
JWT_SECRET="dev-jwt-secret-for-local-development-only-change-in-production"

# Server
PORT=3001
NODE_ENV="development"

# CORS設定 - ローカル開発用（即座に動作）
WEB_URL="http://localhost:3000"
ADMIN_URL="http://localhost:3000"
ALLOWED_ORIGINS="http://localhost:3000,http://localhost:19006"
# WEB_URL="https://your-app.vercel.app"
# ADMIN_URL="https://admin.your-app.com"
# ALLOWED_ORIGINS="https://app1.com,https://app2.com"

# API Key（将来の拡張用）
# API_KEY="your-api-key"

# Admin API Token - 開発専用（本番環境では必ず変更）
ADMIN_API_TOKEN="dev-admin-token-for-local-development-only-change-in-production"

# Email (for password reset)
# SMTP_HOST="smtp.gmail.com"
# SMTP_PORT=587
# SMTP_USER="your-email@gmail.com"
# SMTP_PASS="your-app-password"
