{
  "name": "rss-reader",
  "version": "0.1.0",
  "private": true,
  "description": "RSS Reader - A modern RSS feed reader with web and native apps",
  "author": "khirayama",
  "license": "MIT",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "build": "turbo run build",
    "dev": "turbo run dev",
    "lint": "turbo run lint",
    "format": "turbo run format",
    "test": "turbo run test",
    "type-check": "turbo run type-check",
    "clean": "turbo run clean",
    "cost": "npx ccusage@latest -y"
  },
  "devDependencies": {
    "@biomejs/biome": "^1.8.3",
    "prettier": "^3.3.3",
    "turbo": "^2.0.14",
    "typescript": "^5.5.4"
  },
  "packageManager": "npm@10.8.1",
  "engines": {
    "node": ">=18.0.0",
    "npm": ">=9.0.0"
  }
}
